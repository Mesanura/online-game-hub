# Games

每个 `games/<game-id>` 是独立 workspace package。M2 已建立首个 `games/tic-tac-toe` package，包含 exact `gameVersion 1.0.0` manifest、纯 Core、无业务 client 占位入口、规则文档、测试和 golden replay。

游戏 package 的结构与公开子路径以 [Game Plugin 规范](../docs/GAME_PLUGIN_SPEC.md) 为准。
